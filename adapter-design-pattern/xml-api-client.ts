/**
 * Implementation of an api client that works
 * with an xml based api. For brevity, the client
 * exposes only a "get" and "post" method, accepts
 * no headers and payloads, and no responses are
 * returned. We assume all api operations are 
 * addressed by these two methods.
 **/

export class XmlApiClient {
  get(): void {
    // throw new Error('Method not implemented.');
  }

  post(): void {
    // throw new Error('Method not implemented.');
  }
}
